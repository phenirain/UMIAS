<UserControl x:Class="UMIASWPF.View.User.UserEl.AllPostsElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UMIASWPF.View.User.UserEl"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="370">
    <Grid Background="{DynamicResource SecondaryBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ActiveEntriesMonth" Text="Июль 2024" Grid.Row="0" Padding="10"/>
        <ListBox Grid.Row="1" Background="{DynamicResource SecondaryBackground}"/>
        <TextBlock Text="На этот месяц нет записей"  Grid.Row="2" TextAlignment="Center" VerticalAlignment="Top" Foreground="#807F7F" Margin="0,70,0,0"/>

    </Grid>
</UserControl>
